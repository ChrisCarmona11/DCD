% a = fi(valor,signo,LongitudDePalabra, Fracionales);

pi_f= fi(pi,0,12,10);
%se puede guardar la configurción de fi en un NumericType

short=numerictype(0,12,8);

pi_f= fi(pi,short);

% pi_f.bin para ver la representción binaria

